# md2html

A small CLI that converts Markdown to HTML.

## Run

- Install: `npm install`
- Test: `npm test`
- Try it: `echo "# Hello" | node bin/md2html.js`

## Layout

- `bin/md2html.js` — CLI entry point
- `lib/` — implementation
- `test/` — tests, run via Node's built-in test runner
